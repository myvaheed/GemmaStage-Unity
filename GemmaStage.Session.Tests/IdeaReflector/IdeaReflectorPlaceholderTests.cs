namespace GemmaStage.Session.Tests.IdeaReflector;

// Placeholder so the GemmaStage.Session.Tests.IdeaReflector namespace exists
// after the per-role response parser tests were removed during the System1 /
// System2 / IdeaComprehension refactor. New per-role tests will land here.
internal static class IdeaReflectorPlaceholderTests
{
    public static void Run()
    {
    }
}
